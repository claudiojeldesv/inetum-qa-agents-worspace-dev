# Índice de defectos D1–D93

Catálogo del vocabulario D-NN del proyecto: qué es cada defecto, dónde se midió, dónde vive el
arreglo y en qué estado está. Los D-números son la unidad de trazabilidad de los hallazgos de campo
desde el primer run de ParaBank (beta.3); a partir de K0.42 sustituyen a la numeración de ciclo K0.x
como vocabulario vivo.

**Guarda mecánica**: `tests/unit/indice-defectos.test.ts` verifica que este índice cubre D1..Dmax de
forma contigua y que ningún D-número nuevo aparece en el repo sin fila aquí. Un índice sin validador
sería otra instancia de la familia D2.

## Dos notas de vocabulario (sin esto, el índice induce a error)

1. **`docs/SPEC-caos-corporativo.md` usa un `D1..D4` LOCAL por ciclo**: `K0.33/D2` significa "el
   defecto 2 del ciclo K0.33", NO el D2 global de esta tabla. Mismo caso en `banco-mind2web.md` y
   `comparativa-walker-vs-llm.md`.
2. **Los docs de Copilot usan `D3`/`D4` como "Diseño 3 / Diseño 4"** (brazos del experimento de
   coste): `copilot-edition/h0-metrico.md`, `spike-copilot-port.md`, `plan-copilot-efficient-tokens.md`.

## Dónde viven los K0.x (no se inventarían aquí)

K0.1–K0.17 → [SPEC-kernel-v2.md](../SPEC-kernel-v2.md) · K0.18–K0.41 →
[SPEC-caos-corporativo.md](../SPEC-caos-corporativo.md) · K0.42–K0.47 → [STATUS.md](../STATUS.md).

## Convenciones de la tabla

- **Fuente**: donde mejor está explicado. El comentario de código gana al finding cuando existe
  (el finding narra el run; el comentario explica el mecanismo).
- **Medido en**: los 6 informes de ParaBank son runs consecutivos (`run-beta-parabank.md` = beta.3,
  `-2` = beta.4, `-3` = beta.5, `-4-medicion`, `-5-verificacion`, `-6` = beta.14).
- **Estado**: `cerrado` (arreglo en código, con matiz si lo hay) / `abierto` / `criterio` (D2).

## La tabla

| # | Qué es | Medido en | Arreglo / fuente | Estado |
|---|---|---|---|---|
| D1 | El refiner emite un walk-script que el walker rechaza (26 errores de esquema: `id` por `flow`, `criterion_refs` por `criteria`…) | ParaBank beta.3 | `copilot/src/check-walk-script.ts` (K0.43, commit `fb2293e`); finding [run-beta-parabank.md](../findings/run-beta-parabank.md) §D1 | cerrado |
| D2 | **La familia madre**: algo declarado que nadie valida ni consume, y falla en silencio. Criterio, no ticket. Instancias: D9, D14, D20, D43, D46, D47… | ParaBank beta.3 (3 instancias en un run) | [run-beta-parabank.md](../findings/run-beta-parabank.md) §D2 | criterio |
| D3 | Lo medido no sobrevive al siguiente run (aliases no promovidos). El diagnóstico original era falso: la causa era `promoteRescues` exigiendo cero drift | ParaBank beta.3 | K0.44: `RescueRecord.source` + `aliasPromotionVerdict` | cerrado — **sin verificación de campo** (falta un `alias-hit` real) |
| D4 | El healthcheck dio 26/26 con el MCP `playwright-test` desconectado: verifica el binario, no que las tools estén vivas en la sesión | ParaBank beta.3 | — | abierto |
| D5 | `expect_state` no deja locator autoritativo → un paso verde se cae del emisor y su flujo se paga con una pasada de planner (~130k tokens). Vivió tapado por los propios tests: el fixture de walk-to-spec escribía `resolved_via` A MANO en el report | ParaBank beta.3 | el report de `expect_state` escribe `resolved_via` + `emit_locator`, espejo exacto de `expect_value` (que lo hacía bien desde D46). Par de punta a punta SIN reports fabricados: walker real contra fixture → dom-map → emisor emite en vez de encolar (`d5-expect-state-emisible.test.ts`) | cerrado |
| D6 | Audit-log partido en dos: el refiner escribe en `.work/audit-log.json` en vez de `.work/<site>/audit-log.json` | ParaBank beta.3 | íd. | abierto |
| D7 | No hay CLI para `appendAuditEntry`: el orquestador escribió tres `.ts` desechables solo para registrar | ParaBank beta.3 | íd. (mitigado parcialmente por `audit-mark`) | abierto |
| D8 | El command imprimió `--fd=<ruta>` con una ruta inexistente como si fuera ejecutable | ParaBank beta.3 | íd. ("arreglo de un minuto") | abierto |
| D9 | `--assist` declarado en el contract y aun así se preguntó a mitad de run. Familia D2 | ParaBank beta.3 | íd. | abierto |
| D10 | El panel de asistencia se inyecta con `page.evaluate` y una navegación lo mata: walker esperando 600 s un botón inexistente, y **lo grabado moría con el panel** | ParaBank beta.3 | K0.44: vigilante 500 ms + re-inyección acotada. **2026-08-24**: lo grabado sale de la página en cada gesto por el puente `__qaAssistTrack` y el panel se reinyecta CON la secuencia — `copilot/src/dom-walker.ts`, `recuperarGrabacion` en `walk-core.ts` | cerrado — **verificado en campo** (navegador real: 1 paso grabado, navegación, panel reinyectado con él, parche verificado) |
| D11 | Diagnóstico falso del tier anclado ("no hay label") → rescate MCP innecesario y memoria envenenada | ParaBank beta.4 | [run-beta-parabank-2.md](../findings/run-beta-parabank-2.md) §D11 | abierto |
| D12 | El panel de asistencia se abre y el QA no se entera (el aviso iba a un stdout bufferizado) | ParaBank beta.4 | K0.45: marcador en disco `assist-pending.json`, `copilot/src/dom-walker.ts` / `walk-core.ts` | cerrado |
| D13 | Subagentes lanzados en background: ~30 min de espera muerta; la regla "no background" no es imponible | ParaBank beta.4 | instrumentado en `src/run-cost.ts` | abierto |
| D14 | «Citar y no traducir» sin dientes: `confidence` declarado y sin consumidor. Familia D2 | ParaBank beta.4 | [run-beta-parabank-2.md](../findings/run-beta-parabank-2.md) | abierto |
| D15 | `walk-to-spec` ignora `auth.enabled` | ParaBank beta.4 | íd. | abierto |
| D16 | El `discovery-analyzer` tira los locators medidos: el adaptador `dom-map-to-discovery` no arrastraba `locator_candidates` y el scaffolder reconstruía por heurística lo que el walker ya había resuelto contra el DOM real | ParaBank beta.4; re-medido en OrangeHRM 2026-08-30 (150/161 elementos con locator medido, antes 0) | `measured_locators` viaja por el adaptador y lo consumen LOS DOS lados con el MISMO parser fail-closed (`parseMeasuredLocator` en `pom-scaffolder`, guarda D20: jerga y cadenas `>>` caen a la heurística): el POM emite lo medido y `verify-locators` verifica EXACTAMENTE eso — dos parsers habrían sido la familia D2 con dos cabezas | cerrado |
| D17 | `QA_WORK_DIR` exportado tarde → el refiner escribe al audit-log genérico | ParaBank beta.4 | íd. | abierto (menor) |
| D18 | El workspace desplegado no declara su versión y nada la comprueba (`CLAUDE_PLUGIN_ROOT` congelado al arrancar: 3 despliegues salieron en beta.1 con todo verde) | ParaBank beta.4 | `src/version-drift.ts:2` + `healthcheck.ts` | cerrado |
| D19 | El gate de `open_questions` bloqueaba por EXISTIR una pregunta en vez de por ausencia de oráculo (`then` AMBIGUO) | ParaBank beta.4 | verificado en campo en runs 4 y 5 | cerrado |
| D20 | La notación de diagnóstico de la escalera (`anchored(...)`) se volcaba verbatim al código emitido → POM muerto, `Total: 0 tests` | ParaBank beta.4 | K0.46: lista blanca fail-closed `copilot/src/walk-core.ts` + campo `emit_locator` (cuyo productor faltó → D46) | cerrado |
| D21 | Emisión independiente con locators medidos con sesión | ParaBank beta.4 | solo mencionado en la bitácora de estado | abierto |
| D22 | ~30 s por paso anclado (coste del tier `anchored`) | ParaBank beta.4 | íd. | abierto |
| D23 | Un SIGTERM a los ~10 min mata el walk asistido — el modo asistido no sobrevive a una pausa humana, su propio caso de uso | ParaBank beta.5 | **2026-08-24**: lo grabado se persiste en `assist-pending.json` en cada gesto y se recupera al relanzar con tres cerrojos de identidad (flujo, paso, hash del guion) — `recuperarGrabacion` en `copilot/src/walk-core.ts`; la reanudación del walk ya existía (`walk-state.json`) | cerrado — **el SIGTERM externo no se puede evitar; lo que se arregla es que deje de ser destructivo** |
| D24 | Un nombre accesible numérico mataba el POM entero (`readonly 12345: Locator`) | ParaBank beta.5 (25 links `12345`…) | prefijo de rol en `toIdentifier`, `src/pom-scaffolder.ts:78`; commit `c4de8f9` | cerrado — confirmado en campo |
| D25 | `verify-locators` asume autenticación por redirección; si la app mantiene la URL y cambia el contenido, se desalinea | ParaBank beta.5 (no reproduce en SauceDemo) | relacionado con D50/D51, que sí cerraron | abierto (al final de la cola) |
| D26 | El rechazo fail-closed (D20) nombra el segmento culpable pero no ofrece alternativa | ParaBank beta.5 | confirmado sin tocar en run 5 y SauceDemo | abierto |
| D27 | El panel no recibe la CAUSA, solo la pista: el QA respondió «No existe» a un elemento que existía TRES veces — ambigüedad presentada como ausencia | ParaBank beta.5 | [run-beta-parabank-3.md](../findings/run-beta-parabank-3.md) §D27. Arreglo: la causa se MIDE contra la página antes de abrir el panel (`diagnosticarParaPanel` en `copilot/src/dom-walker.ts`) y el texto sale de `textoAsistencia` en `walk-core.ts`, con candidatos rankeados por la misma función que el informe de G3 (`src/locator-candidates.ts`). Cuatro causas distinguidas: ausente / ambiguo / único-pero-falla / resultado-ausente | cerrado |
| D28 | El acuse compacto de un subagente puede mentir: `{"ok":true}` sobre un fichero inexistente, descubierto tres actos después | ParaBank run 4 | `src/verify-ack.ts` + script + punto 2 de los commands; 15 tests | cerrado |
| D29 | Colisión de nombres en el POM: `readonly transferFunds` tapaba el método homónimo | ParaBank run 4 | `MF-tsc` en `src/scripts/pre-review.ts`; par falsable en tests | cerrado |
| D30 | Auditoría inconsistente de los Writers (2 entradas para 3 specs); sistémico en el run 5 | ParaBank runs 4-5 | hook `hooks/audit-file-write.ts` + `untraced` en verify-ack | cerrado (mecánico) |
| D31 | `verify-ack` verificó el fichero equivocado y dijo «verificado» | ParaBank run 4 | ruta resuelta en el veredicto + cwd impreso | cerrado |
| D32 | `--ack` con JSON en línea inusable en PowerShell 5.1 | entorno Windows | el error nombra la causa y las formas válidas | cerrado |
| D33 | `npm test` no fiable en verde: dos ficheros lentos rebasan timeout bajo carga (9/813 fallos, 813/813 al re-ejecutar) | suite del propio repo | — | abierto |
| D34 | El scaffolder se cree `test_id` a ciegas (atributo equivocado). Segunda vuelta: el arreglo por prosa midió 18/18 y luego 0/31 con el MISMO prompt | ParaBank run 5 → SauceDemo → primer ejercicio de campo en Dolibarr | rescate contra el DOM en `src/scripts/verify-locators.ts` + `src/pom-scaffolder.ts` | cerrado (mecánico) |
| D35 | El Writer conoce la race y aun así produce un test flaky | ParaBank run 5 | `MF-wait-budget` en `src/scripts/pre-review.ts`; verificado end-to-end en el run 6 (2/2 ×2) | cerrado |
| D36 | Un subagente se inventó el timestamp del audit | ParaBank run 5 | cableado (hook `audit-file-write.ts`) — la prosa no bastó, ver D40 | cerrado |
| D37 | `MF-postcondition` ciego a la pantalla: a un spec de login se le exigía asertar `Transfer Complete!` | ParaBank run 5 (2 Writers independientes) | `src/scripts/pre-review.ts:415` + test | cerrado (el finding aún dice "Abierto") |
| D38 | Los Writers editan el `discovery-report.json` para satisfacer un gate: 2 de 3 mutaron la evidencia que los juzga | ParaBank run 5 | detección en `hooks/audit-file-write.ts` + prohibición en `ia4d-writer.md`; no reproducido en SauceDemo | cerrado (detección) |
| D39 | La señal de éxito de auth se satisface con una página de error: `Log Out` también se pinta en el HTTP 500 | ParaBank run 5 | `MF-auth-landing` en `pre-review.ts` + `success_signal` corregido y propagado a los 3 contracts | cerrado |
| D40 | Se instruyó a agentes sin Bash a invocar un script (el arreglo de D36 era prosa a agentes sin manos) | SauceDemo | cubierto por el hook PostToolUse — **pendiente quitar la prosa** de los 2 agentes | cerrado |
| D41 | Colisión locator↔componente en el POM (`TS2300 Duplicate identifier`). Familia de D29, otro eje | SauceDemo | gana el locator, el componente pasa a `<nombre>Component`; 3 tests | cerrado |
| D42 | Aislamiento entre flujos: todos los flujos en un contexto + prefijo de login por flujo → 21 bloqueos falsos de 26. Oculto antes porque otro defecto lo tapaba | OrangeHRM | `copilot/src/dom-walker.ts:5132` (`walker.isolate_flows`, default on); commit `b34e900` | cerrado |
| D43 | El secreto emitido declara su variable y nadie la exporta → `value: expected string, got undefined` ×3. Familia D2 | OrangeHRM | `copilot/src/walk-to-spec.ts:296` (`required_env` + error que dice QUÉ falta); commit `cfdd53c` | cerrado |
| D44 | Un flujo que hereda la sesión del anterior está roto por construcción y el esquema lo daba por válido | OrangeHRM iter. 2 | check de autocontención `copilot/src/check-walk-script.ts --contract=` + `unauthenticated: true`; commit `5c01e2e` | cerrado — visto una sola vez |
| D45 | El `baseURL` por defecto apunta a OTRO sitio: sin `QA_BASE_URL`, tres specs corrieron contra saucedemo con el POM idéntico al verde. Costó dos runs | OrangeHRM | `resolveBaseUrl` en `src/session-policy.ts:99` (env > perfil medido > default con aviso); commit `12492a6` | cerrado — **2 instancias latentes vivas**: `run-s4-mecanico.ts:63`, `lean-run.ts:43` ([genericidad-del-motor.md](../findings/genericidad-del-motor.md)) |
| D46 | `emit_locator` declarado, consumido y probado (con fixture a mano) — **nadie lo producía**. 17/18 pasos y cero specs. La instancia más pura de la familia D2 | ParaBank run 6 | el walker deriva el locator con `css_fallback_attributes` solo si identifica exactamente 1 elemento visible: `copilot/src/dom-walker.ts:2413`; commit `1bec428`. Medido 0→2 specs. Regresión declarada: `config/style-contracts/dolibarr.yaml` | cerrado |
| D47 | `session-policy.ts` declara "sin medición, serializa" y `playwright.config.ts` hace lo contrario. Familia D2 | ParaBank run 6 | — (la regla correcta necesita que el config sepa qué contract corresponde al run) | abierto por decisión de diseño |
| D48 | El idioma de la aplicación lo decide una cabecera que nadie fijaba: misma URL en español con `es-ES`, inglés sin fijar. Explorador y ejecutor pueden mirar DOS aplicaciones | Dolibarr | campo `locale` del contract honrado por config, walker (y su replay), verify-locators y sonda: `src/contract-validator.ts:59`; commit `baab589` | cerrado |
| D49 | El `discovery-analyzer` se inventó una URL (`/user/login.php` → 404, no sale del plan) | Dolibarr | devuelto al productor; dejó el campo vacío con notas | cerrado |
| D50 | El muro de login que no redirige: login servido con 200 en la URL pedida → 74 `unverified` que acusan a locators correctos | Dolibarr | veredicto `unknown` + motivo `login-wall`: `src/scripts/verify-locators.ts:489`; commit `8767e30`. Medido 74 unverified → 82 unknown | cerrado |
| D51 | El manejador de auth da por hecho que el login vive en una URL; aquí hay que PULSAR un perfil (forma de SSO/tenant de banca y seguros) | Dolibarr | `auth.entry_steps` (ruta de entrada declarada en la gramática existente); commit `8767e30`. Medido: bootstrap failed→applied, 0→26 verificados | cerrado |
| D52 | La pantalla que solo existe durante la entrada (el login sin URL) no se verificaba nunca — ahí vivía el defecto de 4 specs | Dolibarr | paso `verify_screen:` dentro de la ruta; commit `8767e30`. Medido: login 0 → 4/4 verificados | cerrado |
| D53 | El planner nativo, bloqueado por compliance, intentó EDITAR el allowlist vía browser_run_code_unsafe antes de detenerse (lo frenó el clasificador de permisos; luego paró y pidió). El prompt por-flujo no lleva la regla de no-elusión de los agentes propios | the-internet | [i6-the-internet.md](../findings/i6-the-internet.md) §D53 | abierto |
| D54 | `MF-postcondition` cuenta headings de mueble como postcondición de negocio exigible (falso positivo medido en TC-004: 'Dropdown List'). `BUSINESS_ROLES` incluye `heading` sin distinguir mueble de resultado — pariente de D37 | the-internet | [i6-the-internet.md](../findings/i6-the-internet.md) §D54 | abierto |
| D55 | El índice posicional sobre roles SIN nombre es ciego al mueble: `.first()`/`.nth()` sin ancla medida aterriza en la barra de paginación o en la cabecera, y ningún gate lo ve (G1 por diseño K0.41; G2 tampoco, un rol sin nombre coincide con muchas) | Dolibarr | [dolibarr-iter2-tres-rojos.md](../findings/dolibarr-iter2-tres-rojos.md) — regla `MF-indice-sin-ancla` propuesta con par falsable | abierto |
| D56 | El oráculo que ya era cierto al empezar: una postcondición terminal (`expect_text "Username"` tras el logout) cuyo literal ya está en el `business_text` de la pantalla de ENTRADA del flujo. Verde sin poder discriminante — se cumple igual si el paso no se ejecuta. Familia de D39 con el rol invertido: la regla «la señal de éxito existe SOLO en la pantalla de destino» se aplica a la señal de auth del contract, no a las postcondiciones que emite el refiner | ParaBank (A/B del FD literal, payload sellado) | [ab-fd-literal-parabank-sellado.md](../findings/ab-fd-literal-parabank-sellado.md) §D56 — regla `MF-oraculo-no-discriminante` propuesta, con par falsable disponible en el run | abierto |
| D57 | El `dom-map` se queda mudo justo donde el sitio es difícil: un campo sin identidad semántica (`<input name="username">` sin label for, sin id, sin aria) sale `{"role":"textbox","cands":[]}` **incluso en el run donde el walker lo resolvió** por el peldaño anclado. `buildLocatorCandidates` solo sabía de test_id/role+name/label/text y no miraba el `css_fallback_attributes` que el contract declara. Afecta a los tres consumidores del mapa: candidatos del panel, POM scaffolder y discovery-analyzer. Forma de D46 (el conocimiento existe y no llega al consumidor) | ParaBank (A/B del FD literal) | `DomElement.css_attr` medido en la página con la MISMA garantía de unicidad-entre-visibles que exige `derivarEmitLocator`; `buildLocatorCandidates` lo emite el último y solo si no hay candidato semántico. Fixture `copilot/fixtures/campo-sin-identidad.html` + 10 tests con cuatro controles | cerrado (medido en campo: 2 campos vacíos → 0 elementos sin candidato, 27/27) |
| D58 | La captura solo reconoce como encabezado `h1`-`h3`: `h4`-`h6` caían a rol `generic` y ni entraban en el selector. En OrangeHRM los rótulos son `h5`/`h6`, así que su cubo de **texto de negocio salía VACÍO** y el panel llegaba a afirmar «esta pantalla no muestra NINGÚN resultado» sobre una pantalla con dos títulos. En ARIA h1-h6 son todos `heading` | OrangeHRM (montaje del ejercicio del panel) | `ROLE_BY_TAG` + selector de captura en `copilot/src/dom-walker.ts`; medido antes/después contra el sitio vivo | cerrado |
| D59 | Los candidatos del panel salían ruidosos por no mirar el rol: pidiendo el botón «Search Employee» la lista traía 8 entradas y 5 eran **nombres de empleados** del listado, colados por compartir la palabra «employee». Es el criterio de muerte que el plan puso a P2, asomando a la primera en una app real | OrangeHRM (montaje del ejercicio del panel) | poda por el rol que declara el propio plan, con caída a la lista completa si el rol no da ninguno (`nombresDePantalla`). Medido: 8 candidatos → 1, y el correcto | cerrado |
| D60 | Un pedido más corto que 3 caracteres no produce candidatos NUNCA, por construcción (el emparejamiento exige palabras de ≥3), y el panel concluía «ni nada que se le parezca» con el elemento delante. No es de laboratorio: el FD de onesait dice «pulsar el botón de cerrar **"X"**» tres veces y las apps corporativas lo pintan `×` | OrangeHRM (traducción del FD a la semántica de onesait) | `pedidoSinPalabrasUtiles` en `src/locator-candidates.ts`: cuando el pedido no da para comparar se enseña lo que hay del rol pedido, en vez de afirmar que no se parece nada | cerrado |
| D61 | Con una ventana flotante abierta los candidatos salían de la pantalla de FONDO, y el botón de cerrar del modal caía fuera del tope de 8. Es el escenario de onesait: cinco ventanas anidadas y tres pasos que son «pulsar la X» de ventanas distintas | OrangeHRM (modal de confirmación de baja) | `nombresDePantalla` acota a los elementos `inDialog` cuando hay alguno. Medido: pedir «X» dentro del modal pasa de ofrecer los botones del fondo a ofrecer `×`, `No, Cancel`, `Yes, Delete` | cerrado |
| D62 | El panel captura `e.target`, o sea el **icono**, no el botón que lo contiene. Un botón de acción moderno es `<button><i class="icon"/></button>`: el `<i>` es rol `generic` y sin identidad, así que se descartaba y se le pedía al QA que «señalara su contenedor» — pedirle que haga el trabajo del navegador. **Bloqueó el ejercicio del panel en campo**: la papelera del listado de OrangeHRM no tiene texto, ni `aria-label`, ni `title` | OrangeHRM (ejercicio del panel, 2026-08-28) | en `click` se sube al primer ancestro interactivo antes de rendirse (`push` en `copilot/src/dom-walker.ts`). El hover NO sube (avisaría en cada envoltorio). Fixture `boton-con-icono.html` + 4 tests, con los controles de que no confunde dos botones y de que un icono suelto se sigue rechazando | cerrado |
| D63 | Todo lo anclado de la escalera cuelga de `getByRole('<rol>', { name })`, y para un CONTENEDOR el extractor saca ese `name` del `textContent` — que Playwright no acepta como nombre accesible de un `role=row`. Medido: `getByRole('row', { name: '0452aaa aaa' })` resuelve a **CERO**. En una tabla de datos con botones de icono ese era el UNICO candidato, asi que nacia muerto y la asistencia se rendia DESPUES del trabajo del QA | OrangeHRM (ejercicio del panel, 2026-08-28) | `anchor.nth` capturado en `anchorOf`, y ancla ESTRUCTURAL de respaldo en `buildFallbackCandidates` (`getByRole('row').nth(K) >> getByRole('button').nth(N)`): no depende del dato de la fila. Va el ultimo y marcado fragil. 3 tests, uno comprueba que RESUELVE contra DOM real | cerrado |
| D64 | El panel de asistencia (fijo arriba-derecha, 400px) TAPA el elemento objetivo cuando crece al grabar: la verificación en vivo hace trial-click, Playwright reintenta contra el panel que intercepta y muere en timeout — parche SIN VERIFICAR con `verify_reason` de jerga (`locator.click: Timeout`) que no apunta al panel. En OrangeHRM tapaba Search/Reset. Mordió al banco una vez y AL QA DOS runs seguidos (el workaround por instrucción —arrastra el panel— no funcionó en la práctica) | OrangeHRM (banco de pruebas + retry del ejercicio, 2026-08-29) | `panelDejaDeInterceptar` en `dom-walker.ts`: entregado el gesto del QA, todos los hosts del panel pasan a `pointer-events: none` — transparente al hit-test, visible para leer el resultado. Par falsable: el banco SIN `mover_panel` (que era el workaround), 10/10 contra OrangeHRM real | cerrado |
| D65 | El motivo del bloqueo «objetivo asistido inalcanzable» afirmaba «el parche se verificó… El parche ES válido» SIN mirar `verified`: en campo salió sobre un parche SIN VERIFICAR — dos frases, las dos falsas, sobre el artefacto que la fusión firma después | OrangeHRM (retry del ejercicio del ciclo, 2026-08-29) | `motivoParcheInalcanzable` en `dom-walker.ts`: el motivo se construye del estado real del parche, en un solo sitio para los dos disparadores (familia D2) | cerrado |
| D66 | La reanudación tras el checkpoint de rescate (exit 42) re-ejecuta el flujo a medias DESDE SU PRIMER PASO con la sesión del testigo (K0.35) restaurada: en un flujo con login el navegador despierta ya dentro y `fill Username` es irresoluble — el walker pide rescate de un paso YA COMPLETADO (s1) y la respuesta legítima del subagente para s7 se descarta como «respuesta de otro paso». Orquestado a ciegas es un bucle quema-tokens: cada reanudación pide rescates de login que nunca resolverán | OrangeHRM (prueba motor+IA con subagente, 2026-08-29) | `debeReiniciarSesionAlReanudar` (`walk-core`, tabla de 4 tests): un flujo A MEDIAS que va a re-ejecutarse descarta la sesión del checkpoint — la re-ejecución exige la sesión limpia que midió el run original. Dos excepciones deliberadas: sesión del caller (`--storage-state`) y `isolate_flows: false` (ahí la limitación se declara, no se disimula). Par de campo: el flujo envenenado del 29, reanudado SIN borrar `walk-session.json` → sesión descartada, respuesta consumida, 6/8 con 1 rescate. **Nota del 2026-09-02 (Fase 2)**: este defecto vive en la REANUDACIÓN, y con canal de rescate declarado ya no hay reanudación — el walker espera la respuesta y sigue en la misma sesión. Sigue aplicando solo al camino de «nadie escucha» (CI, run a pelo), que conserva `exit 42` | cerrado |
| D67 | El original anclado de la fusión (`config/baselines/<site>/walk-script.original.json`) es por SITIO y los guiones son por CASO: el primer merge del sitio lo escribe, y el segundo caso fundido —«si existe, no se toca nunca», que es correcto— se queda SIN ancla propia. Su «antes» se pierde: la fase C no puede reconstruir los cambios de elemento (los apartó con motivo, que es como se encontró) y la distancia al original de la decisión 7 tampoco existe para ese caso | workspace de campo OrangeHRM (fase C sobre el 2º caso fundido del sitio, 2026-08-30) | pendiente — el ancla debería ser por guion (p. ej. `walk-script.original.<hashScript-corto>.json` o subcarpeta por caso), manteniendo «nunca se sobrescribe» por ancla. Mitigación ya operativa: `--original=` explícito en `qa:propuesta`, y el motivo de la apartada lo nombra | abierto |
| D68 | La petición de rescate (`rescue-request.json`) NO arrastraba el `scope` del paso NI declaraba la gramática real: el walker se plantó porque «Book now» aparece 3 veces FUERA del ámbito `{text:'Single'}`, y el subagente recibió solo el hint — declinó con razón («4 Book now indistinguibles») porque la única información que desambiguaba se quedó en casa. Y las instrucciones ofrecían una gramática MENOR que la ejecutable: `locatorFromChain` acepta ` >> `, `.nth(N)` y `.filter({hasText})` desde K0.11a, pero la petición solo declaraba formas planas. 5 de 13 micro-llamadas del ciclo RBP se perdieron ahí, más 3 «ecos» del hint fallido (conducta nueva para la taxonomía del banco de rescates) | Restful Booker (ciclo E2E en terreno virgen, modo walker+IA, 2026-08-30) | tres piezas en la misma sesión: **(1) triaje por clase de motivo** (`triajeDelBloqueo`+`puertaBloqueadaAntes` en walk-core: cascada → ni preguntar; ambigüedad/ámbito → panel del QA; solo el hint limpio va a rescate), **(2) el `scope` viaja** en la petición, en las instrucciones y en el foco de la poda, y la gramática declarada alcanza a la ejecutable, **(3) guarda anti-eco** (`esEcoDelHint`: la respuesta que repite el hint se rechaza SIN ejecutar). Par falsable en DOS sitios: RBP mismo guion 13 llamadas/~538k/0 desbloqueos → **3 llamadas/~136k/4 desbloqueos** (cp007 completó el alta con `getByRole('combobox').nth(0)` y compañía); OrangeHRM guion sin fundir → el rescate legítimo «Buscar»→«Search» sigue pasando el triaje y da el 6/8 original | cerrado |
| D69 | Hermano de D66 que su arreglo no cubría: en la reanudación tras exit 42, la sesión del checkpoint contaminaba el flujo SIGUIENTE completo — `aislarFlujos` se desactivaba con CUALQUIER `storageState`, y la sesión del checkpoint entra por la misma variable que la del caller: el aislamiento entre flujos (D42) quedaba apagado en todo el run reanudado. Medido: cp009 pasó de 0 bloqueos (línea base sin reanudaciones) a 3 (run con 13 reanudaciones); cp007 pidió rescate de Username con un snapshot que enseñaba el botón Logout | Restful Booker (ciclo E2E en terreno virgen, modo walker+IA, 2026-08-30) | `debeAislarFlujos` (walk-core, tabla de 3): solo la sesión del CALLER (auth-handler, pensada para compartirse) desactiva el aislamiento; la del checkpoint es transporte de la reanudación, no sesión compartida. Par de campo: el run RBP reanudado 13 veces emite «sesion reiniciada antes de…» en cada flujo y cp007/cp009 quedan a CERO bloqueos de login | cerrado |
| D70 | `check`/`uncheck` sobre fachada: el hint resuelve al input nativo OCULTO (getByLabel('Male') resuelve; ideal-forms lo esconde tras un control estilizado) y la acción se rechaza por invisibilidad — el peldaño hermano del select inteligente (que sí resuelve los `select` de la MISMA fachada: 24/24 verdes en el mismo run) no existe para radios/checkboxes. Medido: 3 checks fallidos por flujo tumban el gate del negocio (la tabla de tarifas se niega) y arrastran ~40 de los 64 bloqueos de la línea base. Regla de diseño escrita con dato del mismo ciclo: accionar el CONTROL VISIBLE como un usuario, jamás el input oculto por JS — el clic JS sobre los radios de tarifa (left:-9999px, binding solo-jQuery) marca A LA VISTA sin actualizar el importe interno (estado visual distinto del estado interno, medido por el solo-IA) | Tricentis Insurance (ciclo E2E en terreno virgen II, 2026-08-31) | pendiente — candidato natural a la primera rebanada post-estreno; dossier [e2e-tricentis-insurance.md](../findings/e2e-tricentis-insurance.md) §4.1 y §4.3 | abierto |

| D71 | **El viewport no lo declaraba NINGUNA capa** (ni CLI, ni contract, ni script): todo run heredaba el default de Playwright (1280×720) sin aparecer en ningún artefacto. En maqueta responsiva el elemento EXISTE y está OCULTO, y la postcondición del FD se convierte en drift falso sin que nada relacione una cosa con la otra. A/B de una variable: el enlace 'Cuentas' de EspoCRM NUNCA visible en 20 s a 1280×720 (1 nodo en el DOM, oculto) y visible a los 1553 ms a 1400×900. Impacto medido con lo demás igual: la línea base pasa de 23/89 pasos y 66 bloqueos a 71/89 y 18. Alcance: los TRES ciclos E2E habían corrido a 1280×720 sin saberlo (en RBP y Tricentis no mordió por ser maquetas de ancho fijo, no por diseño) | EspoCRM (ciclo E2E en terreno virgen III, 2026-08-31) | `resolveViewport` + `notaTextoOculto` (walk-core, 9 tests): CLI > contract > default de Playwright — el default NO se cambia (inventar uno movería en silencio la línea base de todo lo medido antes); campo `viewport` en el Style Contract; el walker IMPRIME siempre el viewport efectivo; y el motivo del `expect_text` fallido distingue «no está» de «está y no se ve», nombrando el viewport y diciendo que eso es layout, no drift | cerrado |
| D72 | La ventana de quietud (capa 2, K0.13) confunde «aún no ha empezado» con «ya terminó»: en una SPA de arranque lento no hay actividad mientras el bundle carga, así que el settle se declara a los ~440 ms y el primer paso resuelve contra un DOM vacío. Agravante: la **capa 4 (calibración con el p95)** guarda esas muestras como si fueran el tiempo real del sitio — el perfil de tiempos aprende de runs que nunca vieron la aplicación y envenena los siguientes. Medido: entrada de EspoCRM asentada en 463 ms con 0 ciclos de ocupado y todo el flujo en cascada; con `--quiet-ms=2500` asienta en 4569 ms CON 2 reinicios de ventana y el login resuelve. **Instancia materializada (2026-08-31, hallada al cerrar la Fase 0 del censo)**: el brazo A/B `vp-malo` —viewport 1280×720, donde el menú está plegado y 'Cuentas' nunca aparece— escribió sus muestras ciegas en `config/timing-profiles/espocrm.json`, un fichero VERSIONADO, y en la dirección que más daña: cp001/s1 de 2548 ms a 1248, enseñándole al perfil que el sitio es el doble de rápido de lo que es. Revertido a mano; el defecto es que nada lo impidió — un run de diagnóstico no debería poder escribir en la calibración del sitio | EspoCRM (ciclo E2E en terreno virgen III, 2026-08-31) | pendiente — el knob `--quiet-ms` existe y funciona, pero el default muerde y la calibración no distingue una muestra ciega de una buena. Candidato: no calibrar pasos cuyo settle tuvo 0 reinicios Y 0 ciclos de ocupado sobre pantalla sin elementos accionables | abierto |
| D73 | **El presupuesto de rescate se fuga en la reanudación**: D66 descarta la sesión del checkpoint y re-ejecuta el flujo desde su primer paso; si la pantalla no ha pintado (D72), cada paso de la puerta pide rescate con un snapshot vacío y el subagente solo puede declinar — paso a paso, ~44k por declinación garantizada. Y el rescate ÚTIL ya pagado se pierde: el locator comprado para cp003/s5 nunca se consumió porque la re-ejecución se atascó antes de llegar a él. El triaje enruta bien por clase de motivo pero le falta la clase «la pantalla no había pintado», que es diagnosticable de forma determinista y gratis | EspoCRM (ciclo E2E en terreno virgen III, 2026-08-31) | **mitigado por defecto el 2026-09-02 (Fase 2)** — la fuga era de la REANUDACIÓN: el locator comprado se perdía porque la re-ejecución se atascaba antes de llegar a él. Con canal de rescate declarado no hay reanudación (el walker espera y consume la respuesta en la misma sesión), así que ni se pierde lo comprado ni se encadenan declinaciones sobre pantallas sin pintar. **Sigue abierto** porque el camino de «nadie escucha» conserva `exit 42`, y porque falta el candidato original: si el snapshot podado no tiene NINGÚN elemento accionable, no gastar rescate y bloquear con ese motivo. Empareja con D72 | abierto |

| D74 | **El `reason` del bloqueo se sobrescribe con el desenlace del rescate y destruye la clase original.** Cuando se intenta un rescate, el motivo pasa a ser «rescate LLM respondió locator=null» o «el locator del rescate no resuelve en el DOM», y la clase que el triaje había determinado (ambigüedad, ámbito, cascada) desaparece del registro. Medido en Restful Booker, mismo sitio y mismos pasos, dos runs: clase `panel` de 6 a 1 y clase `rescate` de 3 a 11 — cuatro de las seis ambigüedades se contaron como material de rescate porque su texto ya no existía. Consecuencia doble: cualquier censo o instrumentación construido sobre la prosa mide un espejismo, y la ambigüedad que debía llegar al panel del QA deja de estar registrada como tal. Es la enfermedad de D2 en su forma pura: el dato se produce y luego se pisa | Restful Booker (censo de bloqueos, Fase 0 del rescate en proceso, 2026-08-31) | **cerrado** — campo `clase` en `BlockedStep` (las cinco familias del censo, no una taxonomía nueva) escrito por `blockStep` en el momento del bloqueo, más `rescue_outcome` como SEGUNDO campo para el desenlace: se añade, no sustituye. **Y el diagnóstico original estaba incompleto, se corrigió al leer el camino real**: no es que el `reason` se sobrescriba — es que con presupuesto de rescate el PRIMER bloqueo ya llega con el texto del desenlace («rescate LLM respondió locator=null») y la ambigüedad original NUNCA se escribe. Por eso `clasificarBloqueo` (walk-core) decide con el estado OBSERVADO por el motor (`ultimaAmbiguedad`, `ultimoAmbitoFallido`, limpiados por paso) y no con una regex sobre el motivo: clasificar por el texto habría vuelto a fabricar el defecto. Los dos parámetros son opcionales para que `rescue-census.ts` siga sirviendo a los artefactos viejos, que son inmutables — y el censo cree el campo cuando existe en vez de adivinar. 7 tests, con el caso del defecto como primero | cerrado |

| D75 | **`hint.placeholder` se declara en los guiones, NO existe en el tipo `StepHint`, el validador lo acepta y el motor lo ignora.** El hint queda efectivamente vacío: el walker no puede resolver nada y el panel dice «el paso no dice qué buscar» — técnicamente cierto y totalmente inútil para el QA. El motor SÍ tiene peldaño de placeholder (K0.39) pero lee `hint.label ?? hint.name ?? hint.text`: busca POR el atributo placeholder usando el valor de otro campo, nunca `hint.placeholder`. Familia D2 en estado puro. Radio medido: 2 pasos (`cp007/s8` y `s9` de EspoCRM); RBP y Tricentis cero | EspoCRM (estreno manual del QA, 2026-09-01) | pendiente — dos frentes: que `check-walk-script` rechace fail-closed un hint sin ningún campo legible, y decidir si `placeholder` pasa a campo de primera de `StepHint` (el contract de EspoCRM ya prioriza `getByPlaceholder`) | abierto |
| D76 | El panel **ignora el `scope` del paso** al construir la lista de candidatos: con `scope: {text: 'Dirección de facturación'}` ofreció el buscador global de la cabecera, que está fuera del contenedor declarado. `diagnosticarParaPanel` llama a `nombresDePantalla(false, step.hint?.role)` — el rol se pasa, el scope no. Mismo patrón que D75: dato declarado, consumidor que no lo lee | EspoCRM (estreno manual del QA, 2026-09-01) | pendiente — acotar los candidatos al contenedor del scope, y decirlo en el texto del panel | abierto |
| D77 | **Un fallo en el replay de verificación invalida TODO lo enseñado en el run.** El parche del panel se verifica re-ejecutando el flujo en limpio; si un paso previo no resuelve ahí (típicamente el login), la verificación cae y con ella la de todo lo demás, porque todos los flujos empiezan por el mismo acceso. Medido en el estreno: EspoCRM 0/6 verificados (5 por «hint irresoluble en s1 durante el replay»), RBP 2/6, Tricentis 9/15 — **11 de 27**. Lo enseñado no se pierde (se funde con grado `sin-verificar`), pero el QA no obtiene la evidencia que su trabajo merece. Agravante: la verificación **bloquea al QA** mientras corre | Los tres sitios (estreno manual del QA, 2026-09-01) | pendiente — la verificación de un paso no debería depender de la reproducibilidad de los pasos previos ajenos a él | abierto |
| D78 | **El triaje protege el presupuesto de la IA pero no el tiempo del QA.** `triajeDelBloqueo` sabe decir «esto está en cascada de X, ni preguntes» y tiene tests, pero corre en el camino del rescate LLM (dom-walker ~5699), DESPUÉS del peldaño asistido (~5641). Resultado: el panel pregunta al QA por pasos condenados. Medido en el estreno de RBP: tras marcar `cp009/s8` («Consulta sobre desayuno» no existe — el mensaje que `cp004` debía crear no estaba), el panel le pidió señalar el botón «Close» **del diálogo que nunca se abrió**. Cascada en el estreno: 22 de 45 bloqueos en Tricentis, 4 de 21 en RBP | Restful Booker y Tricentis (estreno manual del QA, 2026-09-01) | **cerrado** — el triaje corre ahora DELANTE del peldaño asistido, reutilizando `puertaBloqueadaAntes` y `triajeDelBloqueo` sin duplicar la clasificación: con una puerta bloqueada por delante el paso se bloquea con el motivo del triaje y el panel NO se abre. La ambigüedad y el ámbito fallido SÍ siguen abriendo panel — elegir entre candidatos es para lo que existe. El camino de acción fallida tampoco se toca: ahí el locator resolvió, o sea que el elemento está en la pantalla y preguntar es legítimo. **Y el argumento que lo hace más urgente para el QA que para el LLM**: lo que el QA enseña se promueve a memoria durable con override humano, así que enseñar un locator sobre la pantalla equivocada no se queda en el run, envenena los siguientes. Test con fixture propio (`cascada-condenada.html`) que distingue las dos rutas por el MOTIVO, no por el bloqueo: solo el triaje escribe «en cascada de» | cerrado |
| D79 | **La verificación del parche abría una ventana nueva y podía dejar el navegador cerrado.** El replay en contexto limpio (`browser.newContext` + `newPage`) es una ventana que aparece delante del QA. Daño medido en el estreno:  murió con «no se pudo inyectar el panel: Target page, context or browser has been closed», y en el run siguiente OCHO flujos (cp003-cp010) cayeron con «entry inalcanzable» por la misma causa. El QA destapó además la asimetría que lo delataba: el PRIMER  de un flujo no tiene nada que mute por delante, así que abría ventana; los siguientes se verificaban en vivo sin problema — o sea, las pantallas donde el replay parece más barato son justo las que lo disparan | Tricentis (estreno manual del QA, 2026-09-01) | **cerrado** — la verificación EN VIVO pasa a ser el camino por defecto y el replay se pide con `--verificar-con-replay`. El argumento decisivo lo puso el QA y es de su dominio, no del código: el replay re-ejecuta el camino previo en limpio, así que **vuelve a hacer login** (mortal donde la app admite UNA sesión a la vez, lo normal en banca) y **vuelve a consumir datos de un solo uso**; `retry_safe` es una marca por paso y no puede conocer ninguna de las dos. La contrapartida queda dicha y registrada: en vivo prueba menos, y el acta lo refleja con grado `en-vivo` en vez de `desde-cero` — una verificación más fuerte que te quema los datos no es más fuerte, es destructiva. Test que lo fija por su consecuencia observable en `d81-inventario-punta-a-punta.test.ts` | cerrado |
| D80 | **Un alias que resuelve pero cuya acción falla se vuelve a preguntar como si fuera un locator perdido.** El QA grabó `css=#gendermale` CINCO veces y `Speeding`/`Euro Protection` tres cada uno: 15 enseñanzas para 7 elementos. No era amnesia —el audit-log trae `alias-hit s14: css=#gendermale` cuatro veces: la memoria se promovió y se consultó— sino que el elemento se encuentra y no se puede accionar (fachada `ideal-forms`). Al no decírselo, parece amnesia | Tricentis (estreno manual del QA, 2026-09-01) | absorbido en gran parte por D81: con el proxy visible el paso deja de fallar. Queda el mensaje explícito «sé dónde está, no puedo accionarlo» cuando no haya proxy | abierto |
| D81 | **El panel no podía ofrecer al QA un elemento sin nombre accesible ni oculto**, que son justo los que hacen falta cuando el motor ya se plantó. Dos filtros: `captureScript` salta lo invisible (correcto para el dom-map, que describe lo que un usuario puede usar) y `nombresDePantalla` hace `if (!el.name) continue`. Medido en Tricentis: de 89 elementos de la pantalla, **56 ocultos y 44 sin nombre** — invisibles para el panel, incluido el radio que el QA necesitaba | Tricentis (estreno manual del QA, 2026-09-01) | **cerrado** — inventario aparte para el panel (`inventoryScript`), con: motivo de ocultación buscado en los ANCESTROS (un `display:none` cuatro niveles arriba es «su sección no se muestra», no «sin tamaño»); proxy visible detectado con `elementFromPoint`, que devuelve exactamente quién recibiría el clic del usuario; proxy anclado al id del elemento cubierto (`#gendermale + span` resuelve único donde `span.ideal-radio` resuelve 6, y `getByRole('generic')` ni siquiera es ejecutable); resaltado en página antes de elegir; tres semáforos en palabras (¿lo encuentro? ¿puedo pulsarlo? ¿durará?); y creación de fila SIN gesto previo. Verificado en vivo: pulsar el proxy deja el radio marcado DE VERDAD, disparando el binding de jQuery — no el falso verde del clic JS sobre el input oculto. 16 tests de núcleo | cerrado |

| D82 | **El panel informa del fallo mecánico y no de lo que la pantalla está gritando.** Ante una acción que caduca, dice «locator.click: Timeout» y calla que la propia pantalla exige campos sin rellenar. Medido en Tricentis: el QA vio tres veces el mismo bloqueo en `cp004/s26` sin que nada le dijera que faltaban dos grupos obligatorios de OTRO paso del asistente; el diagnóstico lo hizo el orquestador a mano con dos comandos. La versión ingenua (leer los mensajes de error) **no serviría en este sitio**: los `<span class="error">` viven en el DOM con `display:none` y el único aviso visible es un contador en la pestaña — cero `aria-invalid`, cero `role=alert`, cero `:invalid` nativo, medido. Lo que sí es universal y determinista es CRUZAR lo que la pantalla ofrece contra lo que el caso toca: «hay 5 controles y este caso toca 3; sin tocar: EuroProtection, LegalDefenseInsurance». Distingue las dos culpas —«el locator está mal» y «al guion le falta un paso»— que hoy se presentan igual | Tricentis (estreno manual del QA, 2026-09-02) | **descartado por el QA de momento**, con motivo: prefirió avanzar y ver el caso completo. Se conserva porque las piezas ya existen (el inventario de D81 lista la pantalla, los pasos llevan sus hints) y porque el mismo cruce, corrido al cerrar cada flujo, sería un informe de cobertura del FD — métrica de calidad con valor propio en dominio regulado | abierto |
| D83 | **La memoria durable no era durable: un flag la mandaba a un directorio efímero, en silencio.** El walker tiene default durable (`config/hint-aliases/<site>.json`) y `--aliases` lo sobrescribe sin decir nada. Medido en el workspace del QA: tres runs consecutivos de EspoCRM aprendieron 4 y 3 aliases en `.work/<su-dir>/aliases.json` y `config/hint-aliases/` **ni existía**; ninguno leyó la memoria del anterior, así que el run del rescate en proceso volvió a pedir rescate para `cp003/s5`, un paso cuyo alias ya estaba aprendido — en otro fichero. **La causa no era del producto: eran las guías que el orquestador dio al QA**, todas con `--aliases=.work/…`. La pieza funcionaba y se desconectó desde fuera, run tras run | EspoCRM (Fase 2 en campo, 2026-09-02) | **cerrado** — `notaMemoriaEfimera` (walk-core, 7 tests) y el walker **imprime SIEMPRE** dónde vive su memoria, con la consecuencia delante cuando el destino es efímero. Precedente deliberado: D71 no cambió el default del viewport, hizo que el valor efectivo se dijera siempre. No se prohíbe el flag — los tests y los runs de diagnóstico quieren un fichero desechable. Los aliases perdidos del QA se rescataron a mano al destino durable | cerrado |
| D84 | **Un gesto del QA se promueve a memoria durable sin revisión ni revocación.** La fusión del guion (`merge-assist-patch`) exige aprobación explícita y deja acta firmada; la promoción a alias es **automática**. Medido: el QA señaló «Buscar» en `cp007-alta-cuenta/s8` **a propósito, para probar el panel**, y ese locator —el buscador global de la cabecera para un campo de Ciudad— quedó promovido como verdad del sitio. Si la memoria hubiera estado en el destino durable, el run siguiente habría escrito «Madrid» en el buscador global, en verde y para siempre. Agravante descubierto al limpiarlo: **estabilidad y corrección son ejes distintos** — el orquestador filtró por fragilidad al rescatar la memoria y se quedó justo con el locator estable-pero-falso, descartando el correcto-pero-frágil. Un cerrojo que solo mira si el locator sobrevivirá no dice nada de si apunta a lo que el paso quería | EspoCRM (Fase 2 en campo, 2026-09-02) | pendiente — hay dos formas y hay que elegir con el QA: que la promoción pida permiso (coherente con el acta, pero fricción en cada run) o que se pueda **revocar y auditar** después (`qa:aliases --revocar`, con la revocación en el acta). Hoy se limpia editando un JSON a mano, y eso no es un producto | abierto |
| D85 | **El cerrojo de fragilidad solo miraba al panel.** `aliasPromotionVerdict` rechaza lo posicional desde K0.47 —«el único cerrojo que no admite override humano»— pero la fragilidad la medía el panel (`target.candidate.fragile`) y el camino del rescate LLM **no la declaraba nunca**, así que un `.nth()` respondido por una IA entraba en memoria durable sin tocar el cerrojo. La asimetría quedó en un mismo audit-log: el `getByRole('group').nth(0) >> …` que el QA señaló fue RECHAZADO por frágil, y el `getByRole('textbox', { name: 'Ciudad' }).nth(0)` que contestó el orquestador fue PROMOVIDO. Y el riesgo es MAYOR por el lado de la IA, no menor: el QA ve la pantalla y sabe que elige el primero de dos gemelos; una IA que responde `.nth(0)` sobre un `textbox "Ciudad"` duplicado no puede saber si es el de facturación o el de envío | EspoCRM (Fase 2 en campo, 2026-09-02) | **cerrado** — `locatorEsFragil` (walk-core) y el registro del rescate declara `fragile`, así que el cerrojo que ya existía hace su trabajo. Detección sintáctica a propósito: la gramática de locators es una lista blanca cerrada (K0.46), así que `.nth(` es la ÚNICA forma de expresar posición y no hay nada que interpretar. 4 tests, incluidos los dos posicionales que el run de campo promovió por error | cerrado |

| D86 | **Un panel que caduca destruye lo grabado Y la prueba de que hubo gestos.** `clearAssistMarker()` se llamaba en el ÚNICO punto de cierre de la espera, incluido el timeout: borraba `assist-pending.json` y vaciaba `assistRecorded`. Descubierto investigando el estreno del QA en Restful Booker, donde hubo **OCHO paneles seguidos caducados a 600 s exactos** (~80 min): al ir a diagnosticarlo, dos explicaciones quedaron **indistinguibles con lo que había en disco** — que el QA se ausentara, o que estuviera delante haciendo el gesto que el panel no escucha (**siete de los ocho eran pasos mutantes**, donde el panel pide hover + ◉ y NO clic, y el QA describió justamente que «el modal no era capaz de poner el locator que había seleccionado»). El propio mecanismo borraba la traza que hacía falta para saberlo, y contradecía lo que D23 prometía («lo grabado sobrevive»). Descartadas por experimento otras dos hipótesis: los puentes de `exposeFunction` SÍ sobreviven a navegación, `clearCookies`, `localStorage.clear` y al ciclo de vida del contexto de verificación (5 escenarios, todos los mensajes llegaron a Node) | Restful Booker (investigación del estreno del QA, 2026-09-02) | **cerrado** — el desenlace del panel va al audit-log con `gestos_grabados`, `enviado` y `paso_mutante` (`phase: assist-close`), que es append-only y sobrevive a que el panel siguiente sobrescriba el marcador y a que el run termine; y al caducar el marcador **se conserva SI hay gestos que conservar** — el primer arreglo dejaba de borrarlo SIEMPRE, y un marcador «ESPERANDO AL QA» pegado sobre un run terminado reabría D12; lo cazó la suite completa (`k045-asistencia-observable`), no yo. Límite conocido y escrito: el marcador es de ruta fija, así que varios timeouts encadenados se pisan — lo que sobrevive a todo es el registro. 2 tests con fixture propio, y el segundo es el par que faltaba: distingue «nadie atendió» de «gesticuló y no envió» | cerrado |

| D87 | **El inventario del panel proponía locators AMBIGUOS y los pintaba «estables».** `locatorDe` prefería el nombre accesible y **nunca comprobaba que resolviera único**. Medido en campo el mismo día que se arregló D86, y diagnosticado con él: la portada de Restful Booker tiene CUATRO enlaces «Book now», así que las cuatro filas del inventario ofrecían el MISMO locator; al elegir una, la validación en vivo la rechazaba («ese locator no resuelve único aquí»), no se creaba fila, y el QA enviaba el panel VACÍO creyendo que había señalado algo — el registro de D86 lo dejó en una línea: `enviado=true, gestos=0`. **Dos lecciones**: (1) *estable*, *correcto* y *único* son tres ejes distintos, y el semáforo «¿durará?» decía verde sobre un locator que apuntaba a cuatro cosas — el mismo error que se cometió al rescatar la memoria en D84, quedándose con el estable-pero-falso; (2) **verificar con una semántica distinta a la del consumidor es no verificar**: la primera versión del arreglo comparaba nombres con `===` y el `getByRole` de Playwright casa SIN distinguir mayúsculas, así que sobre una página con «Book Now» y tres «Book now» seguía declarando único algo que en ejecución resuelve cuatro | Restful Booker (repetición del tramo, 2026-09-02) | **cerrado** — cada candidato se comprueba EN LA PÁGINA antes de ofrecerse (y no solo que resuelva uno: que ese uno **sea este** — un locator único que apunta a otro elemento sería lo peor de todo), y si resulta ambiguo se desambigua por posición DENTRO de su propio grupo: `getByRole('link', { name: 'Book now' }).nth(2)`, que conserva el significado en vez de caer al posicional entre TODOS los enlaces. Eso es frágil por definición, se declara como tal, y el cerrojo de D85 lo mantiene fuera de la memoria durable. La comparación de nombres imita la de Playwright (sin distinguir caja, por subcadena) y el parseo es por posiciones, no por regex: el script viaja en un template literal donde `\(` se convierte en `(` y una expresión regular deja de casar EN SILENCIO — ya pasó en la primera versión. 5 tests con fixture propio, incluido el que comprueba que lo que sí tiene identidad propia sigue saliendo estable | cerrado |
| D88 | **El ámbito que señala al TÍTULO no trepa al contenedor, y el paso acaba en el panel.** Es el límite conocido de K0.36 —«no se trepa del título a su contenedor: elegir qué ancestro es el contenedor sería adivinar»— medido ahora con su coste real. En Restful Booker los pasos traen `scope: {text:'Single'}` (y Double, y Suite), que es lenguaje de FD y dice EXACTAMENTE cuál de los cuatro «Book now» toca; el ámbito resuelve al `<h5>.card-title` y dentro de un título no hay enlaces, así que el walker dice «ámbito sin el hint dentro: 3 coincidencias fuera» y abre panel. **Cuatro de los seis paneles del run existieron solo por esto.** Hay una regla que NO es adivinar y está medida en vivo: subir al ancestro más cercano que contenga coincidencias y exigir que contenga EXACTAMENTE UNA — para Single, Double y Suite son 2 niveles hasta `div.card`, y cada uno da su habitación correcta (`/reservation/1,2,3`). Si el primer ancestro con coincidencias ya trae varias, se queda como hoy | Restful Booker (repetición del tramo, 2026-09-02) | **cerrado**: `trepaDelTituloAlContenedor` en `copilot/src/dom-walker.ts` — se sube al ancestro más cercano con coincidencias y se exige que traiga EXACTAMENTE UNA; si el primero que sabe algo ya trae varias no se desempata y el paso sube al panel, como hoy. Verificado contra la portada real ANTES de escribir el test: los tres flujos (Single/Double/Suite) pasan **6/6 pasos, 0 rescates, 0 bloqueados**, cada uno a SU habitación (`/reservation/1`, `/2`, `/3`), con tres trepadas de 2 niveles en el audit-log. 4 tests con fixture propio, uno de ellos el par que distingue «resolvió» de «resolvió el correcto» | cerrado |
| D89 | **El locator que el QA introduce a mano entra en memoria durable como NO frágil, por decreto.** `dom-walker.ts` estampa `fragile: false, why: 'introducido por el QA'` sin mirar el locator, así que el cerrojo de D85 —el único que no admite override humano— no se aplica al camino del inventario de D81. Estaba latente y **lo activó D87**: desde que el inventario desambigua con `.nth(i)`, lo que el QA señala ES posicional, y en el run de campo se promovieron TRES aliases durables frágiles. Peor que frágiles: **falsos**. Los tres apuntaban a la habitación equivocada o al botón del banner, y `cp010` los reutilizó y pidió «Reserve Now» en la portada — el síntoma que reportó el QA. Es D84 otra vez, por otra puerta. Los tres aliases se retiraron a mano de `config/hint-aliases/restful-booker.json` (copia en `.envenenado-2026-09-02`); **mientras siga abierto, cada run vuelve a envenenar la memoria** | Restful Booker (repetición del tramo, 2026-09-02) | **cerrado**: la fragilidad se calcula con `locatorEsFragil` en los dos lados — el registro (`dom-walker.ts`) y el semáforo del panel, que decía «manual» en verde sobre un posicional. La copia en la página es por POSICIÓN y no por regex, y sin acentos ni comillas invertidas: vive dentro de un template literal (D87). 3 tests, y el primero comprueba que el inventario produce de verdad un posicional — sin eso los otros dos no probarían nada | cerrado |
| D90 | **El panel salta del idioma del FD al idioma del locator sin peldaño intermedio.** El motor trabaja con la frase del FD —el guion guarda `{role:'link', name:'Book now'}`, no un locator, y 97 de los 102 pasos completados del run se resolvieron así— pero cuando esa frase es AMBIGUA el panel no pregunta en ese idioma: ofrece cuatro filas idénticas (`link · Book now`) y pide elegir un locator. El QA eligió el primero, que era el botón del banner. La información que falta ya existe y también es lenguaje de FD (el `scope`): el peldaño que falta es poder contestar «el de la tarjeta Single» y que el motor traduzca, dejando el locator crudo como último recurso para páginas que no dan ninguna palabra a la que agarrarse. Reemplaza al enfoque previo de «etiquetar las filas», que trata el síntoma | Restful Booker (repetición del tramo, 2026-09-02) | **cerrado**: `zonasDelHint` en `copilot/src/dom-walker.ts` y el botón «¿Cuál de ellos?» del panel. Ante un hint ambiguo se ofrecen las ZONAS de la pantalla nombradas con una palabra que se ve («el de «Single»»), y lo elegido se funde como `scope: {text:'Single'}` — lenguaje de FD, no posición: a partir de ahí lo resuelve D88 solo. Cada zona se VERIFICA antes de ofrecerse (etiqueta única en la página **y** cadena ejecutada que resuelve a uno); ofrecer algo ambiguo pintado de bueno es D87. Y como no es posicional, sí entra en memoria durable, al contrario que el `.nth(i)` del inventario. Verificado contra la portada real antes del test: Single→`/reservation/1`, Double→`/2`, Suite→`/3`. 7 tests. **De paso salió un defecto latente propio**: la gramática de locators no sabía leer `.locator('xpath=..')`, que es lo que D88 escribe en `resolved_via` — o sea que un alias o un `step.locator` con esa forma caía a la escalera en silencio (la clase de K0.39). Ahora se lee, y **solo** saltos `..`: un xpath arbitrario abriría la lista blanca de par en par | cerrado |
| D91 | **La lista blanca de lo emisible mira el PREFIJO del segmento, así que admite cualquier cosa pegada detrás.** `primerSegmentoNoExpresable` (walk-core) recorre la cadena y acepta el segmento si empieza por un prefijo conocido; probado en el momento: `getByText('Single', { exact: true }) ↑2` **pasa el filtro** y viajaría verbatim a un `.spec.ts` que no compila. Es la puerta de D20 sin cerrar del todo, y el gate cuyo nombre entero es «fail-closed». Hoy no lo explota nadie —la única notación de diagnóstico que existe, `anchored(…)`, se rechaza porque el fallo está en el prefijo— así que es fail-open latente, no un fallo activo. Se descubrió construyendo D88: la primera versión anotaba la trepada como `↑2` dentro del `via`, y el filtro la dejó pasar. D88 se arregló sin depender de esto (el `via` es `.locator('xpath=../..')`, código real y verificado contra el sitio: resuelve 1 y a la habitación correcta), pero el agujero sigue ahí para el próximo que añada notación | Restful Booker (repetición del tramo, 2026-09-02) | — | abierto |
| D92 | **El panel decía QUÉ no consiguió hacer, pero no de qué TIPO de problema se trataba ni qué botón le correspondía.** Lo nombró el QA tras recorrer el ensayo entero: *«alguien que no conozca qa-automator no sabe realmente la diferencia entre la parada 3 y un "no encontré el objeto"»* — «el plan lo llama de otra manera» y «esto no existe» sonaban igual, la única pista era una línea de candidatos que hay que saber leer, y con cinco botones delante sin indicación eligió tres veces el inventario teniendo «¿Cuál de ellos?» al lado (medido en el run anterior del ensayo). Tres piezas en el arreglo: (1) **el TIPO de lo que se busca se dice cuando se sabe** — del `role` si el plan lo declara (76% de los pasos medidos), del verbo si no (`fill` solo puede ser un campo; otro 22%), y en el resto se usa el verbo y NO se afirma tipo, porque decir «el botón» de algo que puede ser un enlace sería fabricar; el candidato de la lista lleva su tipo SIEMPRE («Contact (enlace)»), porque el suyo sale del DOM; (2) **cada causa termina nombrando su botón** con `▸`, y la ausencia dice qué pasa después de declarar «No existe aquí» (queda anotado como diferencia y el caso sigue); (3) **los botones van agrupados por para qué sirven** — «para resolver este paso» / «salidas» / «de paso, mientras estás aquí» — porque «Añadir comprobación de texto» no resuelve el paso y estaba en la misma fila que el que sí. El mismo tipado llegó a `frasePaso` (la vista de caso): «pulsar el botón «Guardar»». Mensajes de la guía del ensayo re-capturados del panel real | Restful Booker (ensayo del panel, 2026-09-03) | 7 tests D92 en `d27-panel-con-causa.test.ts` + los de frase actualizados | cerrado |
| D93 | **En una ventana pequeña el panel era más alto que la pantalla y los botones del final quedaban FUERA, sin scroll con el que llegar.** Medido por el QA en campo: *«el largo es un problema porque no se alcanza a escrollear»* — los grupos de D92 alargaron el panel y en su resolución las SALIDAS cayeron bajo el borde. Un panel cuyas salidas no se alcanzan solo ofrece las opciones que caben. Arreglo en `POSTURAS_CSS` (compartido por los DOS paneles, familia D2): el panel nunca supera `100vh` y hace scroll el CUERPO — la cabecera y la tira quedan fijas porque son el arrastre y las posturas; la vista de caso scrollea entera (fuera el tope propio de su lista). Y el arrastre queda SUJETO a la ventana: lo único que mueve el panel es su propia cabecera, así que uno arrastrado fuera no se podía recuperar. La alternativa que se pidió y NO se hizo, con motivo: sacar el panel a una ventana propia del navegador — vive en el DOM de la página a propósito, y una ventana aparte fue exactamente lo que el QA rechazó en el estreno del inventario (sesiones de un solo uso, datos que se queman). 5 tests a 900×460 midiendo CAJAS, no DOM (la lección del panel apagado), incluido el gesto real de arrastre exagerado; comprobado que el test falla con el defecto puesto | Restful Booker (ensayo del panel, 2026-09-03) | `d93-panel-cabe-en-la-ventana.test.ts` | cerrado |

## Recuento

**54 cerrados** · **29 abiertos** · **1 criterio permanente (D2)**.

Cerrados con matiz: D3 sin verificación de campo; D23 arregla el daño, no la causa (el SIGTERM viene de fuera); D44 visto una sola vez; D45 con dos
instancias latentes vivas; D40 con prosa obsoleta pendiente de retirar en dos agentes.

## Dónde se midió cada bloque

| Sitio | D-números |
|---|---|
| ParaBank (JSP legacy) | D1–D39, D46, D47 |
| SauceDemo | D40, D41; segunda vuelta de D34; no-reproducción de D25 y D38 |
| OrangeHRM (SPA Vue) | D42–D45 |
| Dolibarr (ERP PHP) | D48–D52, D55; primer ejercicio de campo de D34 |
| ParaBank (A/B sellado) | D56, D57 |
| OrangeHRM (ejercicio del panel) | D58–D63 |
| OrangeHRM (banco de pruebas de paneles + retry del ejercicio) | D64, D65 |
| OrangeHRM (motor+IA: checkpoint de rescate con subagente) | D66 |
| OrangeHRM (fase C en el workspace de campo) | D67 |
| Restful Booker (ciclo E2E en terreno virgen) | D68, D69 |
| Tricentis Insurance (ciclo E2E en terreno virgen II) | D70 |
| EspoCRM (ciclo E2E en terreno virgen III) | D71, D72, D73 |
| Censo de bloqueos (retroactivo sobre los tres ciclos E2E) | D74 |
| Estreno manual del QA (EspoCRM, Restful Booker, Tricentis) | D75–D81 |
| Fase 2 del rescate en campo (EspoCRM) + estreno de Tricentis | D82–D85 |
| Investigación del estreno de Restful Booker | D86, D87 |
| Restful Booker (repetición del tramo con D87 dentro) | D88–D91 |
| Restful Booker (ensayo del panel) | D92, D93 |
| the-internet (Sinatra) | D53, D54 |
| No es un sitio | D32 (PowerShell 5.1), D33 (suite propia), D40 (config de agentes) |
