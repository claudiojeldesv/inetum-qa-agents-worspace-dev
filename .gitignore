# Node
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Env local (machine-specific, posibles secretos)
.env

# Outputs & runtime
/output/
/playwright-report/
/allure-results/
/allure-report/
/test-results/
/.playwright-mcp/

# Local settings (machine-specific)
.claude/settings.local.json

# Upstream reference clone (cloned separately as read-only)
/agent-skills/

# Python venv (validator stub)
/validator/.venv/
**/__pycache__/
*.pyc
.pytest_cache/

# OS
.DS_Store
Thumbs.db

# Audit log local runs (recreated per session)
/audit-log.json
/judge-report.json
/review-feedback.json
/discovery-report.json
/compliance-verdict.json
/drift-report.json
/qa-automator-run-summary.json
/mode-routing.json
# Saved plans del planner nativo (recreados por run; evidencia va a docs/findings/)
/*-plan.md

# StorageState / auth artifacts (session cookies, recreated by login spec at runtime)
/playwright/.auth/

# Empty seed scaffold resembrado por el MCP playwright-test en cada setup_page
/tests/e2e/seed.spec.ts

# Archived pre-MVP / referential content (relocated, not deleted)
/tmp/
