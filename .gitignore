# Generated outputs (test cases, uncertainty reports from agent runs)
/output/

# Personal Claude Code settings (machine-local, not versioned)
.claude/settings.local.json

# Upstream reference clone (cloned separately as read-only)
/agent-skills/

# Python virtualenv local del validador (Bici en adelante)
/validator/.venv/
**/__pycache__/
*.pyc
.pytest_cache/

# Node / TypeScript / Vitest
node_modules/
dist/
coverage/
.vitest/
*.tsbuildinfo
npm-debug.log*
.eslintcache

# Output local del agente (audit trail, no fuente)
audit-log.json

# Cache local del Playwright MCP server (logs de consola, snapshots de páginas)
.playwright-mcp/
